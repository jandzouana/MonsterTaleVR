using UnityEngine;
using System.Collections;

public class sitInSpaceship : MonoBehaviour {
	public bool buttonClicked;

	// Use this for initialization
	void Start () {
		buttonClicked = false;
	}
	
	// Update is called once per frame
	void Update () {
		if(buttonClicked == true)
		// check if player looking at ship and in area aka if the laser is blue
		//if (gameObject.Laser.GetComponent.laserBlueFlag == true) {
			// teleport player (position player)
			// Autowalk off
			Debug.Log ("DownIf");
	}

	//when the player clicks
	//public void PointerClick(){

		}



